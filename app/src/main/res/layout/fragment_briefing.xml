<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_primary"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="MORNING BRIEFING"
            android:textColor="@color/accent_green"
            android:textSize="11sp"
            android:letterSpacing="0.1"
            android:fontFamily="monospace"
            android:layout_marginBottom="4dp" />

        <TextView
            android:id="@+id/tv_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_muted"
            android:textSize="13sp"
            android:layout_marginBottom="20dp" />

        <!-- Task input card -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/card_background"
            android:orientation="vertical"
            android:padding="14dp"
            android:layout_marginBottom="10dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="WHAT ARE YOU WORKING ON TODAY?"
                android:textColor="@color/text_muted"
                android:textSize="9sp"
                android:letterSpacing="0.1"
                android:fontFamily="monospace"
                android:layout_marginBottom="10dp" />

            <EditText
                android:id="@+id/et_tasks"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="80dp"
                android:background="@drawable/input_background"
                android:padding="10dp"
                android:textColor="@color/text_primary"
                android:textColorHint="@color/text_muted"
                android:textSize="14sp"
                android:hint="@string/tasks_hint"
                android:inputType="textMultiLine"
                android:gravity="top" />

            <Button
                android:id="@+id/btn_generate_briefing"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:layout_marginTop="10dp"
                android:text="Generate briefing"
                android:textColor="@color/accent_green"
                android:backgroundTint="@android:color/transparent"
                android:fontFamily="monospace"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton" />

        </LinearLayout>

        <!-- Loading -->
        <ProgressBar
            android:id="@+id/progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone"
            android:layout_marginTop="16dp" />

        <!-- Output -->
        <TextView
            android:id="@+id/tv_briefing_output"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:background="@drawable/briefing_output_background"
            android:padding="16dp"
            android:textColor="@color/text_primary"
            android:textSize="13sp"
            android:lineSpacingMultiplier="1.7"
            android:layout_marginTop="12dp" />

    </LinearLayout>
</ScrollView>
